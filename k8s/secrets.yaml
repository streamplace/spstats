apiVersion: v1
kind: Secret
metadata:
  name: spstats-secrets
  namespace: default
type: Opaque
stringData:
  # IMPORTANT: Replace these with real values before applying.
  # In production, use sealed-secrets, external-secrets, or your cluster's
  # secret management solution instead of checking values into source control.
  tap-admin-password: "CHANGE_ME"

