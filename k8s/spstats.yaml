apiVersion: apps/v1
kind: Deployment
metadata:
  name: spstats
  namespace: default
  labels:
    app: spstats
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spstats
  template:
    metadata:
      labels:
        app: spstats
    spec:
      containers:
        - name: spstats
          image: ghcr.io/streamplace/spstats:latest
          env:
            - name: TAP_URL
              value: "http://tap.default.svc.cluster.local:2480"
            - name: TAP_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: spstats-secrets
                  key: tap-admin-password
            - name: CLICKHOUSE_URL
              value: "http://clickhouse.default.svc.cluster.local:8123"
            - name: CLICKHOUSE_DATABASE
              value: sp_stats
            - name: RELAY_URL
              value: "https://relay1.us-east.bsky.network"
            - name: DID_SCRAPE_INTERVAL_MS
              value: "1200000"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://tempo.default.svc.cluster.local:4318/v1/traces"
            - name: LOG_LEVEL
              value: "info"
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi

