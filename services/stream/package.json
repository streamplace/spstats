{
  "name": "stream",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --import=dotenv/config main.ts",
    "start": "node --import=dotenv/config main.ts"
  },
  "dependencies": {
    "@atproto/tap": "^0.0.3",
    "@clickhouse/client": "^1.6.0",
    "@sp-stats/telemetry": "workspace:*"
  }
}
